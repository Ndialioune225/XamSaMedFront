export const environment = {
  // En production, l'API est servie sur la même origine (reverse-proxy → Laravel).
  // Adaptez cette valeur à votre déploiement réel.
  apiUrl: 'http://localhost:8000/api',
};
